#!/bin/sh
# -*- shell-script -*-
#$Id: brkpt3.tests,v 1.1 2008/08/22 19:07:59 rockyb Exp $

[ -z "$srcdir" ] && srcdir=$PWD
[ -z "$builddir" ] && builddir=$PWD

if test -z $top_builddir ; then
  top_builddir=../..
fi

cmdfile="$srcdir/../data/brkpt3.cmd"
debugged_script="$srcdir/../example/subshell.sh"

$SH ${top_builddir}/bashdb -B -q -L $top_builddir -x $cmdfile $debugged_script
