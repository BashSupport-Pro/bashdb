#!/bin/bash -f
# -*- shell-script -*-
#$Id: sopts.tests,v 1.9 2008/08/21 13:49:04 rockyb Exp $

[ -z "$srcdir" ] && srcdir=$PWD
[ -z "$builddir" ] && builddir=$PWD

. ${builddir}/check-common.sh

quitcmd="${top_srcdir}/test/data/quit.cmd"
contcmd="${top_srcdir}/test/data/quit.cmd"
debugged_script="${top_srcdir}/example/dbg-test1.sh"

$SH ${top_builddir}/bashdb -B -q -L .. -x $contcmd -c 'echo test1'
# Note -B needs to come before -e
$SH ${top_builddir}/bashdb -B -e -q --library .. -x $contcmd -c 'echo not run'
