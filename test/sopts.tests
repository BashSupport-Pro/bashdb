#!/bin/sh
# -*- shell-script -*-
#$Id: sopts.tests,v 1.4 2008/08/09 02:36:18 rockyb Exp $

[ -z "$srcdir" ] && srcdir=$PWD
[ -z "$builddir" ] && builddir=$PWD

. ${builddir}/check-common.sh

quitcmd="${srcdir}/data/quit.cmd"
contcmd="${srcdir}/data/quit.cmd"
debugged_script="${srcdir}/dbg-test1.sh"

$BASH ${TOP_BUILDDIR}/bashdb -B -q -L .. -x $contcmd -c 'echo test1'
# Note -B needs to come before -e
$BASH ${TOP_BUILDDIR}/bashdb -B -e -q -L .. -x $contcmd -c 'echo not run'
