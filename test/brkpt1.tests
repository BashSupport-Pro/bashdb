#!/bin/sh
# -*- shell-script -*-
#$Id: brkpt1.tests,v 1.4 2008/08/10 00:54:15 rockyb Exp $

[ -z "$srcdir" ] && srcdir=$PWD
[ -z "$builddir" ] && builddir=$PWD
d="$srcdir/.."

if test -z $top_builddir ; then
  top_builddir=..
fi

cmdfile="$srcdir/data/brkpt1.cmd"
debugged_script="dbg-test1.sh"

$BASH ${top_builddir}/bashdb -B -q -L .. -x $cmdfile $debugged_script
