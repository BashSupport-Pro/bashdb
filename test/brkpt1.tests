#!/bin/sh
# -*- shell-script -*-
#$Id: brkpt1.tests,v 1.6 2008/08/19 22:10:45 rockyb Exp $

[ -z "$srcdir" ] && srcdir=$PWD
[ -z "$builddir" ] && builddir=$PWD
d="$srcdir/.."

if test -z $top_builddir ; then
  top_builddir=..
fi

cmdfile="$srcdir/data/brkpt1.cmd"
debugged_script="$srcdir/example/dbg-test1.sh"

$SH ${top_builddir}/bashdb -B -q -L .. -x $cmdfile $debugged_script
