#!/bin/sh
#$Id: test-brkpt,v 1.3 2008/08/08 22:04:19 rockyb Exp $
# -*- shell-script -*-

[ -z "$srcdir" ] && srcdir=$PWD
[ -z "$builddir" ] && builddir=$PWD

. ${builddir}/check-common.sh

tests=${@:-"brkpt1 brkpt2 brkpt3"}
for SUBTEST in $tests ; do
  echo "checking $SUBTEST..."
  TEST_NAME=$SUBTEST
  . ${builddir}/check-common.sh
  TEST_FILE="$builddir/${TEST_NAME}.check"
  RIGHT_FILE="$srcdir/data/${TEST_NAME}.right"
  (cd $srcdir && ${THIS_SH} ${TEST_NAME}.tests > $TEST_FILE 2>&1 < /dev/null)
  check_output $TEST_FILE $RIGHT_FILE
  rc=$?
  if test $rc -eq 0  ; then 
    rm -f $TEST_FILE
  else
    exit $rc
  fi
done

# Return code tells testing mechanism whether passed or not.
exit 0
